apiVersion: v1
kind: Service
metadata:
  name: litellm-service
  namespace: kigawa-net-llm-proxy
  labels:
    app: litellm
spec:
  ports:
    - name: http
      port: 80
      targetPort: 4000
      protocol: TCP
  selector:
    app: litellm
  type: ClusterIP
