apiVersion: v1
kind: ConfigMap
metadata:
  name: oauth2-proxy-config
  namespace: argocd
data:
  client-id: mcp-argocd
  # Keycloak クライアント作成時にこの値をクライアントシークレットとして設定する
  client-secret: changeme-replace-with-actual-secret
